# Single Producer Single Consumer Queue

## Build

```shell
git clone https://github.com/panmasuo/spsc
cmake -S . -B build
cmake -build build
```

## Run

Example application:

```shell
./build/spsc
```

Unit tests:

```shell
./build/tests/unit_tests
```
